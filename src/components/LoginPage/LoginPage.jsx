import AuthForm from "components/AuthForm";
// import s from "./LoginPage";

const LoginPage = () => {
  return (
    <div>
      <AuthForm type="login" />
    </div>
  );
};

export default LoginPage;
